{
  "name": "location-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "CAP full stack location tracker with SAPUI5 frontend and SAP HANA persistence",

  "scripts": {
    "start": "node server.js",
    "dev": "cds watch",
    "hybrid": "cds watch --profile hybrid",
    "build": "cds build",
    "deploy": "cds deploy --to hana",
    "build:mta": "mbt build",
    "deploy:btp": "cf deploy mta_archives/location-tracker_1.0.0.mtar"
  },

  "dependencies": {
    "@sap/cds": "^8",
    "@cap-js/hana": "^1.9.2",
    "@sap/xssec": "^4",
    "express": "^4"
  },

  "devDependencies": {
    "@sap/cds-dk": "^8",
    "@cap-js/sqlite": "^1",
    "cds-plugin-ui5": "^0.13.0"
  },

  "cds": {
    "folders": {
      "db": "db",
      "srv": "srv",
      "app": "app"
    },

    "requires": {
      "db": {
        "kind": "sqlite",

        "[production]": {
          "kind": "hana",
          "impl": "@cap-js/hana",
          "deploy-format": "hdbtable"
        },

        "[hybrid]": {
          "kind": "hana",
          "impl": "@cap-js/hana",
          "deploy-format": "hdbtable"
        }
      },
      "auth": {
        "[production]": {
          "kind": "xsuaa"
        }
      }
    }
  },

  "workspaces": [
    "app/*"
  ]
}
